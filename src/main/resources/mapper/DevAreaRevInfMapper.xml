<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.enough.issuebe.mapper.DevAreaRevInfMapper">

    <select id="selectDevAreaRevList" resultType="com.enough.issuebe.domain.DevAreaRevInf">
        SELECT
            DEV_AREA_CD AS devAreaCd,
            OWNER_CD AS ownerCd,
            USAGE_CD AS usageCd,
            FINAL_REV AS finalRev,
            REV_STATUS AS revStatus,
            REV_DESC AS revDesc,
            CREATE_USER AS createUser,
            CREATE_DT AS createDt,
            UPDATE_USER AS updateUser,
            UPDATE_DT AS updateDt
        FROM DEV_AREA_REV_INF
        WHERE DEV_AREA_CD = #{devAreaCd}
        ORDER BY FINAL_REV DESC, OWNER_CD
    </select>

</mapper>
